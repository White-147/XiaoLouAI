# 安全策略

## 支持范围

当前仅维护 `main` 分支和最近一次公开发布内容。历史实验分支、保留迁移材料、本地运行记录和未脱敏运营数据不在公开安全支持范围内。

## 漏洞报告方式

如发现安全问题，请优先使用 GitHub 仓库的 **Report a vulnerability** / Security Advisories 私密入口提交。若仓库暂未启用该入口，请先提交一个不包含漏洞细节的 Issue，说明影响范围和联系方式，等待维护者私下沟通。

请不要在公开 Issue、PR、README 评论或提交记录中直接粘贴：

- 可复现攻击步骤、PoC 或漏洞利用代码
- API Key、模型 Provider 密钥、支付凭证、对象存储凭证、Token、Cookie、私钥
- 用户上传素材、生成结果、企业申请资料、订单数据、数据库 dump 或生产日志

## 数据与密钥约束

XiaoLouAI 面向 AI 创作平台和公开访问部署链路，运行时可能接触 Provider 配置、对象存储、支付回调、账户数据和生成媒体。任何截图、日志、任务记录和验证证据都必须先脱敏；`.runtime/`、`deploy/local-secrets/`、真实运营数据和私有凭据不应提交到 Git。

## 处理预期

维护者会先确认问题是否影响当前公开代码，再决定修复方式、披露范围和是否需要在 README、运行记录或发布说明中补充风险提示。
