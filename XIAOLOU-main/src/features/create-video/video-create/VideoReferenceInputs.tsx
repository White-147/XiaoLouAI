import type { ReactNode } from "react";

type VideoReferenceInputsProps = {
  children: ReactNode;
};

export function VideoReferenceInputs({ children }: VideoReferenceInputsProps) {
  return <>{children}</>;
}
